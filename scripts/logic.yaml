script:
  - id: update_vpd_display
    then:
      - lvgl.label.update:
          id: vpd_value_label
          text: !lambda |-
            static char buf[16];
            snprintf(buf, sizeof(buf), "%.2f", id(ha_vpd).state);
            return buf;